import 'package:flutter/material.dart';

class ParkingManagerDasgboard extends StatelessWidget {
  const ParkingManagerDasgboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [Text("Parking Manager Dashboard")],
      ),
    );
  }
}